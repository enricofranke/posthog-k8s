PostHog {{ .Chart.AppVersion | trunc 12 }} (upstream PostHog/posthog) is being deployed.

UI (app listener):     {{ .Values.posthog.siteUrl }}
Ingest (SDK traffic):  {{ include "posthog.publicUrl" . }}
Router service:        {{ include "posthog.serviceName" (list . "router") }} (ports app={{ .Values.router.ports.app }}, ingest={{ .Values.router.ports.ingest }})

First start takes several minutes: ClickHouse and Kafka come up, then the migrate job applies the
schema, then web/worker become ready. Watch it with:

  kubectl -n {{ .Release.Namespace }} get pods -l app.kubernetes.io/instance={{ .Release.Name }} -w
  kubectl -n {{ .Release.Namespace }} logs job/{{ include "posthog.serviceName" (list . "migrate") }} -f

Try it without an ingress:

  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "posthog.serviceName" (list . "router") }} 8080:{{ .Values.router.ports.app }}
  open http://localhost:8080   (set posthog.siteUrl accordingly for cookies to work)

{{- if and .Values.postgresql.bundled (not .Values.postgresql.external.host) }}

NOTE: the bundled PostgreSQL is a single instance without backups. Use CloudNativePG or a managed
database for anything you care about (see postgresql.external in values.yaml).
{{- end }}
{{- if and .Values.objectStorage.bundled.enabled (not .Values.objectStorage.endpoint) }}
NOTE: session replays and exports are stored in the bundled single-node SeaweedFS. Point
objectStorage.endpoint at S3-compatible storage for durability.
{{- end }}
{{- if not .Values.posthog.existingSecret }}
NOTE: secrets were generated by the chart and kept in Secret {{ include "posthog.envSecretName" . }}.
Back it up: losing ENCRYPTION_SALT_KEYS makes encrypted fields unreadable.
{{- end }}
