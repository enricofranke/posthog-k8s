<clickhouse>
    <!-- Chart addition: allow native BACKUP/RESTORE to a local path (used for schema seeds
         and available for your own backups: BACKUP DATABASE posthog TO File('/var/lib/clickhouse/backups/<name>')). -->
    <backups>
        <allowed_path>/var/lib/clickhouse/backups/</allowed_path>
    </backups>
</clickhouse>
